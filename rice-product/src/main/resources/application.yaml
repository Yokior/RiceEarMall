# 应用服务 WEB 访问端口
server:
  port: 10000


spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/rice_pms
    driver-class-name: com.mysql.cj.jdbc.Driver


mybatis-plus:
  global-config:
    db-config:
      id-type: auto