# 应用服务 WEB 访问端口
server:
  port: 11000


spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/rice_wms
    driver-class-name: com.mysql.cj.jdbc.Driver


mybatis-plus:
  global-config:
    db-config:
      id-type: auto